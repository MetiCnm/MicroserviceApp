<nav class="navbar navbar-expand-lg navbar-light bg-dark ">
  <div class="container-fluid align-items-baseline">
    <div class="brand_data">
      <h5 class="navbar-brand text-white">Traffic Application</h5>
    </div>
    <div class="logged_data d-flex align-items-center">
      <div class="d-flex ms-1">
        <% if current_user %>
          <a class="navbar-brand text-white"><%= current_user.name.concat(" ").concat(current_user.surname) %></a>
        <% else %>
          <a class="navbar-brand text-white">Not logged in</a>
        <% end %>
      </div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse text-white ms-1" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if current_user%>
            <% if current_user.role == 'Administrator' %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-white fs-5" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Administration
                </a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                  <li><%= link_to "Notifications administration", notifications_path, :class => 'dropdown-item'%></li>
                  <li><%= link_to "Vehicles administration", vehicles_path, :class => 'dropdown-item' %></li>
                  <li><%= link_to "Fines administration", fines_path, :class => 'dropdown-item' %></li>
                </ul>
              </li>
            <% end %>
          <% end %>
        </ul>
      </div>
      <div class="d-flex">
        <% if current_user %>
          <%= link_to "My profile", user_path(current_user), :class => 'btn btn-primary ms-2' %>
          <%= button_to "Logout", logout_path, :method => "delete", :class => 'btn btn-danger ms-2' %>
        <% else %>
          <%= button_to "Login", login_path, :method => "get", :class => 'btn btn-primary ms-2' %>
        <% end %>
      </div>
    </div>
  </div>
</nav>